#%RAML 1.0 DataType
type: object
properties:
  id?: string # this will be a uuid
  from: string
  to: string
  subject: string
  body: string
  sentOn?: datetime
  status:
    type: string
    enum: [delivered, pending, error]
    required: true